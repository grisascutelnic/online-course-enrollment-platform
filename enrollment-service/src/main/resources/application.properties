spring.config.import=optional:file:.env[.properties],optional:file:enrollment-service/.env[.properties],optional:configserver:http://localhost:8888

spring.application.name=enrollment-service
server.port=8082

services.course-service.url=http://localhost:8081

spring.data.mongodb.uri=mongodb://localhost:27018/enrollment_db

jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION}